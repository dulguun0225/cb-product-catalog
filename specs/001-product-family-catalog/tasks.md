# Tasks — Product family catalog

| | |
|---|---|
| **Feature** | `001-product-family-catalog` |
| **Authored** | `2026-09-02` |
| **Gate** | none. Automated consistency check only, at every tier. |

## Derived from

| Artifact | sha256 |
|---|---|
| `spec.md` | `db96798df5684fbe494ee33aa5cebd29dbcd8306d5a938c417b842ba42967383` |
| `plan.md` | `9d5f3fa8a883ad4b9a32b8132ee8ed269ff9278a9e4702281786ccf86b7de866` |

Every task below builds files under `backend/` only. `deploy/dev/docker-compose.yml` is under
neither workspace and has **no task**: plan §2 and §9 assign it to the DEPLOY stage.

## Phase 1 — The `backend/` module, its schema and its identifiers

- [x] **T-001** Create the Maven module: `backend/pom.xml` on `spring-boot-starter-parent` 3.5.16 with `maven.compiler.release` 21, the committed wrapper (`backend/mvnw`, `backend/mvnw.cmd`, `backend/.mvn/wrapper/maven-wrapper.properties` pinned to Maven 3.9.16), and every dependency pinned to the version plan §9 names — jOOQ 3.21.7, `jooq-meta-extensions` 3.21.7, Flyway 13.4.0 + `flyway-database-postgresql` 13.4.0, springdoc-openapi 2.9.0, `java-uuid-generator` 5.2.0, Testcontainers 1.21.4 + `postgresql` 1.21.4, `archunit-junit5` 1.5.0. No range, no `LATEST`. Plus `ProductCatalogApplication.java`, `config/TimeConfig.java` exposing one `Clock` bean, and `application.yml` setting `spring.threads.virtual.enabled=true` and `logging.structured.format.console=ecs`.
  [FR-003] [NFR-002] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/config/StructuredLogFormatTest.java` · *Evidence:* `./mvnw -B verify` runs to completion on a clean clone; every captured log line parses as one JSON object; the `Clock` bean is injectable and no domain class calls `Instant.now()`
- [x] **T-002** Write `backend/src/main/resources/db/migration/V1__product_family.sql` creating `product_family` exactly as plan §5 declares it — `id uuid primary key` with **no** column default, `family_code varchar(20) not null` behind a named unique index `ux_product_family_code`, `name varchar(120) not null`, `status varchar(16) not null` with a check constraint on `ACTIVE|RETIRED`, `created_at`/`updated_at timestamptz not null` — and wire `jooq-codegen-maven` to generate from the committed migration files via `DDLDatabase` in the `generate-sources` phase. `Depends: T-001`
  [FR-003] [FR-012] [FR-018] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/persistence/MigrationSchemaIT.java` · *Evidence:* a Testcontainers `postgres:16.15-alpine` applies the migration and the test asserts the unique index and the check constraint exist by name; `target/generated-sources/jooq` holds the generated table class and is not committed
- [ ] **T-003** Write `backend/src/main/java/mn/netgroup/cb/productcatalog/ids/FamilyIds.java`: the one type in the repository that produces an identifier, wrapping `com.fasterxml.uuid.Generators.timeBasedEpochGenerator()`. `UUID.randomUUID()` appears nowhere. `Depends: T-001`
  [FR-002] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/ids/FamilyIdsGoldenTest.java` · *Evidence:* a golden test pins the emitted layout — version nibble 7, variant bits, and the leading 48-bit timestamp field increasing across successive calls under a fixed clock

## Phase 2 — Persistence seam, domain rules and the error catalog

- [ ] **T-004** Write `persistence/Tx.java` — the one lambda-scoped transaction seam, `read(Function<DSLContext,T>)` and `write(Function<DSLContext,T>)`, with no injectable `DSLContext` anywhere — and `persistence/ProductFamilyRepository.java` with explicit jOOQ DSL statements only: `insert`, `findById` (`fetchOptional`), `findPage(status, familyCode, id, limit)` ordering `(family_code, id)` ascending, and `retireIfActive(id, updatedAt)` as a guarded `UPDATE ... WHERE id = ? AND status = 'ACTIVE'`. No attached-record CRUD, no `fetchOne`, no plain-SQL string. `Depends: T-002`
  [FR-001] [FR-005] [FR-006] [FR-007] [FR-008] [FR-010] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/persistence/ProductFamilyRepositoryIT.java` · *Evidence:* a Testcontainers integration test inserts, reads back, pages across a seeded set with duplicate names, filters by status, and asserts `retireIfActive` affects one row on `ACTIVE` and zero on `RETIRED`
- [ ] **T-005** Write `domain/FamilyCode.java` (a value type whose only factory rejects anything outside 3–20 characters of `A`–`Z` and `0`–`9`), `domain/FamilyStatus.java`, `domain/ProductFamily.java`, and `domain/ProductFamilyService.java` with `create` (identifier from `FamilyIds`, timestamps from the injected `Clock`, status `ACTIVE`, duplicate-code integrity violation on `ux_product_family_code` translated to the duplicate outcome) and `retire` (guarded update; on zero affected rows re-read and return the row unchanged when it is already `RETIRED`, and report not-found when it is absent). Nothing writes `status = 'ACTIVE'` after insert and nothing updates `family_code`. `Depends: T-004`
  [FR-001] [FR-010] [FR-011] [FR-012] [FR-013] [FR-016] [FR-017] [FR-018] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/domain/ProductFamilyServiceIT.java` · *Evidence:* the test covers create, a rejected malformed code, a rejected over-length name, a duplicate code, retire from `ACTIVE`, retire again returning the same `updatedAt`, and two concurrent retires producing one transition
- [ ] **T-006** Write `api/error/ErrorCode.java` — the compile-checked catalog enum holding `FAMILY_CODE_INVALID`, `FAMILY_NAME_INVALID`, `FAMILY_CODE_DUPLICATE`, `FAMILY_NOT_FOUND`, `LIMIT_ABOVE_MAXIMUM`, `CURSOR_INVALID`, `STATUS_FILTER_INVALID`, `INTERNAL_ERROR`, each with its HTTP status and typed parameter names — and `api/error/ProblemAdvice.java`, one `@RestControllerAdvice` extending `ResponseEntityExceptionHandler`, the only place in the repository an error body is built. The unknown-throwable branch emits `INTERNAL_ERROR` with a correlation identifier and nothing else. `Depends: T-001`
  [FR-015] [FR-019] [FR-023] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/api/error/ErrorCatalogSnapshotTest.java` · *Evidence:* a committed snapshot of every `(code, status, param-names)` triple that the test diffs, plus a leak test asserting no exception message, class name or stack frame reaches the body of a `500`

## Phase 3 — The HTTP operations

- [ ] **T-007** Write `api/ProductFamilyController.java` create and read operations — `POST /v1/product-families` returning `201` with `Location: /v1/product-families/{id}`, and `GET /v1/product-families/{id}` — plus the request and response records with `createdAt`/`updatedAt` serialised as RFC 3339 UTC `Z` instants. No `PATCH` mapping anywhere. `Depends: T-005, T-006`
  [FR-001] [FR-004] [FR-005] [FR-019] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/api/ProductFamilyCreateReadIT.java` · *Evidence:* the test asserts `201` with the `Location` header, a round-trip read of every field, a `409` problem document on a duplicate code, a `400` on a malformed code and on an over-long name, a `404` on an unknown identifier, and that every instant ends in `Z`
- [ ] **T-008** Write `api/KeysetPager.java` and `api/CursorCodec.java` — the only class rendering a paginated query and the only class encoding a cursor (HMAC-sealed, carrying the sort spec `familyCode,id`) — and the `GET /v1/product-families` operation returning `{ items, nextCursor }` with `limit` defaulting to 20, a hard maximum of 100, and an optional `status` filter. Above-maximum `limit` is rejected, never clamped. `id` appears in no declared sort vocabulary. `Depends: T-007`
  [FR-006] [FR-007] [FR-008] [FR-009] [FR-020] [FR-021] [FR-022] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/api/ProductFamilyListIT.java` · *Evidence:* the test pages a seeded set of 25 families to exhaustion asserting no row is skipped or duplicated, asserts `nextCursor` is null only on the last page, and asserts `400` for `limit=101`, for a tampered cursor, for a cursor issued under a different sort spec, and for `status=DRAFT`
- [ ] **T-009** Write the `POST /v1/product-families/{id}/retire` operation on `api/ProductFamilyController.java`, returning `200` with the family body. There is no operation that leaves `RETIRED` and no operation that accepts a family code after creation. `Depends: T-007`
  [FR-010] [FR-011] [FR-013] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/api/ProductFamilyRetireIT.java` · *Evidence:* the test retires an `ACTIVE` family and asserts `200` with `status: RETIRED`, retires it again and asserts an identical body including `updatedAt`, and asserts `404` for an unknown identifier

## Phase 4 — Contract artifact, gates, health and packaging

- [ ] **T-010** Wire the two build gates. First, the contract artifact: `config/OpenApiConfig.java` supplying the document metadata with `springdoc.api-docs.version=openapi_3_1`, `backend/src/test/java/.../contract/OpenApiNormalizer.java` as the one hand-owned normaliser (recursive key sort, pinned array-element order, LF endings, trailing newline), and the generated, normalised document committed at `backend/contracts/openapi.yaml`. Second, the ban list as one executable ArchUnit test class covering every construct plan §9's guardrail row names: injectable `DSLContext`, attached-record CRUD, `fetchOne`/`fetchAny`, plain-SQL `String` constructs, wall-clock calls in `domain` and `persistence`, `orderBy` touching `id` outside `KeysetPager`, field and setter injection, `@Transactional`, `@Scheduled`, `@Async`, `@Cacheable`, `@PatchMapping`, `UUID.randomUUID()` outside `FamilyIds`, and any error body built outside `ProblemAdvice`, with the generated jOOQ packages excluded and every entry either enforced by a named rule or listed in the class as deferred with a reason. `Depends: T-008, T-009`
  [FR: n/a — two build gates that carry no behaviour of their own: the committed contract and its drift check, mandated by `java-backend-api` *One committed OpenAPI document, generated and diffed*, and the ban-list class mandated by `java-backend-rules` *The ban list is an executable test class*] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/contract/OpenApiContractDriftTest.java`, `backend/src/test/java/mn/netgroup/cb/productcatalog/BanListTest.java` · *Evidence:* the drift test boots the application, regenerates the document **twice** under two different default time zones and locales, normalises both, and fails `./mvnw verify` unless both regenerations and the committed file are byte-identical; the ban-list class fails `./mvnw verify` when any banned construct is introduced, and a meta-assertion reconciles its declared entries against the rules present in both directions
- [ ] **T-011** Enable the health operation and package the service: `management.endpoints.web.exposure.include=health` with the datasource indicator on, so `GET /actuator/health` reports readiness; and `backend/Dockerfile` from `eclipse-temurin:21-jre-alpine` copying `target/*.jar`, with `ARG APP_PORT`, `EXPOSE`, and a non-root user. `Depends: T-001`
  [FR-014] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/api/HealthIT.java` · *Evidence:* the test asserts `200 {"status":"UP"}` against a booted application with a real database, and `./mvnw -DskipTests package` followed by `docker build backend/` produces a runnable image
- [ ] **T-012** Write the two remaining non-functional tests: `perf/GetFamilyLatencyTest.java`, driving `GET /v1/product-families/{id}` at 50 requests per second for 60 seconds against the booted service and asserting the p95 of `http.server.requests`, and `NoCommittedSecretsTest.java`, scanning committed files for credential-shaped values and asserting `.env` is listed in `.gitignore`. `Depends: T-011`
  [NFR-001] [NFR-003] · *Test:* `backend/src/test/java/mn/netgroup/cb/productcatalog/perf/GetFamilyLatencyTest.java`, `backend/src/test/java/mn/netgroup/cb/productcatalog/NoCommittedSecretsTest.java` · *Evidence:* both tests run in `./mvnw verify`; the latency test reports the measured p95 and fails above 200 ms; the secret scan reports zero findings
